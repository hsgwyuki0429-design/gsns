{
  "name": "gsns",
  "version": "0.1.0",
  "private": true,
  "description": "GSNS — TikTok-style feed for playable games. First viewer plays, everyone else watches the replay.",
  "scripts": {
    "start": "node server/server.js",
    "icons": "node scripts/gen-icons.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
